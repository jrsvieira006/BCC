#Escreva a frase
texto = input ("Digite uma frase: ")

#Variavel invertida para armazenamento
texto_invertido = " "

#Caracteres da string
indice = len(texto) -1

#Repetição
while indice >= 0:

    #Pega a letra atual e junta ela na variável
    texto_invertido += texto[indice]
    indice -= 1 
print("A string invertida é:", texto_invertido) 
